uchat:
  storage:
    provider: postgres

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://127.0.0.1:5432/uchat}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:Test.132}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
